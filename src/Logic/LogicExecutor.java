package Logic;

public class LogicExecutor {
	StorageStub mainStorage;
	
<<<<<<< HEAD
	public CommandResults handleAdd(CommandStub command) {
		return new CommandResults();
	}
	
	public CommandResults handleEdit(CommandStub command) {
		return new CommandResults();
	}
	
	public CommandResults handleDelete(CommandStub command) {
		return new CommandResults();
	}
	
	public CommandResults handleView(CommandStub command) {
		return new CommandResults();
	}
	
	public CommandResults handleInvalid(CommandStub command) {
=======
	public static CommandResults handleAdd(CommandStub command) {
		return new CommandResults();
	}
	
	public static CommandResults handleEdit(CommandStub command) {
		return new CommandResults();
	}
	
	public static CommandResults handleDelete(CommandStub command) {
		return new CommandResults();
	}
	
	public static CommandResults handleView(CommandStub command) {
		return new CommandResults();
	}
	
	public static CommandResults handleInvalid(CommandStub command) {
>>>>>>> befd6ffbdccaf3d7c2ae74ec6accc72c0cc3dadc
		return new CommandResults();
	}
}
